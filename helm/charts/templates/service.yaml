apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube2ram.fullname" . }}
  labels:
    {{- include "kube2ram.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube2ram.selectorLabels" . | nindent 4 }}
